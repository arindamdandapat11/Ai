# Server
server.port=8080
spring.application.name=email-assistant

# Database (update with your credentials)
spring.datasource.url=jdbc:postgresql://localhost:5432/email_assistant
spring.datasource.username=postgres
spring.datasource.password=Arindam123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Gemini AI
gemini.api.key=${GEMINI_API_KEY}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# CORS (allow your React frontend)
cors.allowed-origins=http://localhost:5173,http://localhost:8080

# Logging
logging.level.com.cimbaai.emailassistant=DEBUG
logging.level.org.springframework.web=INFO

